# Mode B: SPA is served by the FastAPI backend, so the Gradio URL is derived
# from the current origin at runtime. Leaving this unset is intentional —
# resolveGradioUrl() falls back to window.location.origin + "/gradio".
# VITE_GRADIO_URL=
